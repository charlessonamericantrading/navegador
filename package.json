{
  "name": "navegador-ia-root",
  "version": "1.0.0",
  "description": "Root workspace for AI Agent Browser Native Desktop",
  "license": "MIT",
  "private": true,
  "scripts": {
    "install:frontend": "cd frontend && npm install",
    "install:backend": "node scripts/install-backend.js",
    "install:desktop": "cd desktop && npm install",
    "install:all": "npm run install:frontend && npm run install:desktop && npm run build:engine",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && .venv\\Scripts\\python app/core/main.py",
    "dev:desktop": "cd desktop && npm start",
    "start": "npx concurrently -k -n \"frontend,desktop\" -c \"blue,magenta\" \"npm run dev:frontend\" \"npm run dev:desktop\"",
    "dev": "npx concurrently -k -n \"frontend,backend\" -c \"blue,green\" \"npm run dev:frontend\" \"npm run dev:backend\"",
    "build:engine": "cargo build --manifest-path engine/Cargo.toml -p engine-core --bin engine_server --release --locked",
    "build:app": "node scripts/build-app.js",
    "build:app:python": "node scripts/build-app.js --with-python-backend",
    "bench": "node scripts/bench/run-corpus.mjs"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
